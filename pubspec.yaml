name: sov_inte_forbi
description: Wake My Stop - Never miss your train station again
version: 1.0.0

environment:
  sdk: ^3.7.0
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: any
  firebase_auth: any
  cloud_firestore: any
  http: any
  provider: any
  google_fonts: any
  flutter_map: any
  latlong2: any
  geolocator: any
  flutter_animate: any
  url_launcher: any
  intl: any
  web: any
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
  uses-material-design: true
  assets:
    - assets/
